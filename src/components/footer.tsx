import { Button } from 'antd'
import './footer.css'

function Footer() {
  return (
    <footer className="app-footer">
      <Button type="primary" className="app-footer-goal-btn">
        Add your goal!
      </Button>
    </footer>
  )
}

export default Footer
